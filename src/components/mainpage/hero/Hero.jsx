import React from 'react';
import styles from './hero.module.scss';

const Hero = () => {
    return (
        <div className={styles.hero}>
            히어로
        </div>
    );
};

export default Hero;